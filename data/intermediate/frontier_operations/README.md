# Frontier Operations Support Files

This folder stores package-local support inputs for the Frontier operations figure.

Files in this folder are copied from the legacy project visualization inputs so the
replication package can regenerate the thesis figure without reaching back into
`_visualisations/`.
